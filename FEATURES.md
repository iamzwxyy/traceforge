# TraceForge 功能手册与需求追踪

> 面向使用者的长期文档 · 最近更新：2026-08-29 · 当前阶段：核心成功率与界面减重

TraceForge 是一个本地编程智能体。它的重点不是“能不能生成代码”，而是让同一个任务中的多轮
需求从计划、修改、检查到完成后复核都有可见证据，并且在停止、断线或回滚时保持行为可解释。

本文档回答三个问题：现在能做什么、具体怎么用、下一步在打磨什么。每次用户可见功能或
行为发生变化，都应在同一个提交中更新本文档。

## 五分钟开始使用

运行环境为 macOS 或 Linux，需要 Python 3.12 和 `uv`。

```bash
uv sync --locked --all-extras
uv run traceforge
```

应用会直接启动在 <http://127.0.0.1:8765> 并打开浏览器，不需要先在命令行选择工作区；无界面
环境可使用 `uv run traceforge serve --no-open-browser`。第一次使用时：

1. 打开右上角“模型设置”。
2. 选择 OpenAI 官方、DeepSeek 官方或自定义兼容服务。官方预设会配对正确接口并给出已精确适配的模型建议；OpenAI 地址留空即可，自定义预设保留手动模型和地址。
3. 确认模型并输入 API Key。首次没有凭证时，页面会就地说明下一步并禁用无效的“测试并保存”，不会等到请求失败后才用全局错误提示。
4. 高级设置仍支持引用已有的 owner-only 单行凭证文件，也可以按模型文档手动填写上下文窗口；若环境凭证同时存在，清空文件路径即可在同一步测试中安全回退。
5. 点击“测试并保存”；只有草稿配置中的模型成功完成原生工具调用，Key 才会写入本机应用数据目录下防符号链接的 `0700` 专用目录和新 `0600` 私密文件，配置随后原子保存并启用。

服务预设是低摩擦入口，不是模型白名单。用户仍可直接编辑模型和兼容接口；地址只有精确匹配官方
HTTPS 根路径或 `/v1` 时才沿用官方能力，具体端口号、账号信息、额外路径或相似域名都会自动回到
“自定义兼容服务”，与后端的 fail-closed 能力识别保持一致。

TraceForge 的数据库只保存凭证文件的绝对路径，不保存、回传或展示密钥值；校验错误也会移除原始请求内容。
失败探测不会覆盖上一次已经验证的连接，也不会把失败草稿中的 Key 写入文件或数据库。只点“保存设置”
会明确清除就绪状态；新建、继续和恢复任务都要求当前连接已通过探测，避免把“有凭证”误报成“可用”。

上下文窗口不是固定写死的 64k：每次运行会冻结当时解析出的容量。优先使用手动设置；只有官方
DeepSeek 接口上的精确 V4 模型 ID 才使用内置 100 万目录值；其他模型或自定义网关使用保守的
64k 回退（可通过 `TRACEFORGE_CONTEXT_LIMIT` 调整）。模型设置会显示容量及来源。上下文压力达到
约 80% 时会压缩旧历史，并按 token 预算保留约 16% 的近期消息；工具定义也计入估算，工具调用与
对应结果不会被拆开。

思考强度同样按“官方接口 + 精确模型 ID”解析。创建任务或继续对话时，可从模型真正声明支持的
“模型默认 / 关闭 / 最小 / 低 / 中 / 高 / 极高 / 最大”中逐轮选择；不支持的档位不会出现在页面。
选择器使用紧凑的离散档位胶囊，而不是会暗示连续数轴的滑杆；“模型默认”由模型决定，不表示最低
档。只有一个可信选项时，页面会显示明确的“唯一可用”静态状态。“模型默认”不会向兼容接口发送
猜测字段，自定义网关和未知模型因此只提供该安全选项。模型设置会
展示能力来源和目录版本，避免仅凭名称相似就误判协议。当前 OpenAI 精确目录覆盖 `gpt-5.6`
别名及 Sol/Terra/Luna、`gpt-5.5`、`gpt-5.4`/Mini/Nano、`gpt-5.3-codex` 和 `gpt-5`；
DeepSeek 目录覆盖官方 V4 Flash/Pro/Flash Vision Exp。目录外路由不会靠前缀猜测能力。

## 任务与项目入口

### 直接任务

适合临时任务，不需要先准备目录：

1. 点击左侧矩形“新建任务”。
2. 只输入想要的结果；Enter 开始运行，Shift+Enter 换行。TraceForge 会根据任务范围自动决定是否在实施前请你确认计划；动作权限默认“自动审批”，也可逐轮选择“手动审批”或“完全访问（工作区）”；思考强度默认跟随模型，也可选择当前精确模型支持的档位。
3. TraceForge 会在系统“文档”目录的 `TraceForge` 文件夹下创建
   `traceforge-task-<随机标识>` 独立目录；实际路径会显示在输入区和任务顶部。

直接任务不会复用上一次目录，避免两个临时任务意外读写同一批文件。

### 项目任务

适合持续维护同一个仓库：

1. 点击左侧“添加项目”。macOS 会打开系统原生文件夹选择器；不支持原生选择器的环境会回退到
   TraceForge 自带的目录窗口。
2. 选定目录后会直接进入该项目的任务输入页；项目同时以可折叠文件夹显示在左侧，项目任务嵌套在文件夹下。
3. 点击项目文件夹右侧的加号，在该目录中创建任务；直接任务始终保留在顶层。

打开创建页后，点击任意历史任务卡会立即回到对应运行详情，不需要先点“取消”。
进入任务后可点击“打开目录”，直接在 macOS Finder 中定位该任务目录；Linux 有图形会话且
安装 `xdg-open` 时会打开系统文件管理器，无图形环境则明确提示不支持。

同一工作区同时只允许一个写任务，避免多个 Agent 互相覆盖；不同工作区可以并行运行。

如果所选工作区根目录存在精确命名的 `AGENTS.md`，TraceForge 会在每一轮开始前读取一次，
并在任务标题旁显示中性的“规则 1”标记。规则作为项目作者提供的上下文，统一交给 Planner、
Builder 和 Verifier，并明确标为低于当前用户请求。模型仍可能误解语义冲突；本地强制保证的是它
不能放宽审批、命令沙箱、凭证保护或工作区边界。专用的规则徽标、Trace 和时间线清单只显示
相对路径、字节数和哈希，不直接复制规则原文。

读取结果会作为当前轮不可变快照与轮次原子保存：同任务追问会重新读取，进程中断后的恢复只复用
已保存快照，不会悄悄采用磁盘上的新版本。没有规则也会保存明确的空快照。为保持 v1 语义可解释，
当前只识别工作区根目录的 `AGENTS.md`，不查找主目录、父目录、嵌套目录、include、skill、plugin
或环境文件；符号链接、非普通文件、无效 UTF-8、NUL、凭证形态内容，以及连同安全说明后超过
32 KiB 的文件会在创建轮次前被拒绝。根目录超过 10,000 个直接条目时也会确定性拒绝，避免
目录扫描成本和文件枚举顺序改变结果；规则、当前请求和 Planner 工具若无法共同放入当前模型的
上下文窗口，同样不会先创建一个注定失败的轮次。

这里冻结的是自动发现和注入的规则上下文，不是把 `AGENTS.md` 变成隐藏文件：模型仍可按普通
项目文件读取或编辑当前磁盘内容，但后读到的版本不会替换本轮已标记的规则快照。规则原文会发送给
当前配置的模型服务，模型也可能在回答、计划或工具参数中引用它，因此 `AGENTS.md` 不是保密通道，
不应放置秘密；凭证形态检查只覆盖当前 provider key 与可识别的 `sk-...` 文本。中断期间更换凭证后，
恢复会在出站前用新凭证复查完整运行记录、不可变事件账本、活动决策和整个模型请求，包含只会在紧凑 JSON 边界拼成
凭证的情况；同步预检会把已封存规则插入到实际消息位置后再检查，因此规则尾部与历史消息头部拼接
出的凭证也会在创建异步任务前被拒绝，任务保持“已中断”。冲突时不会发起模型或工具调用，也不会改写旧规则快照。此时可以改回旧凭证恢复，或
明确“停止”：专用原子恢复会放弃活动决策，把不再可安全持久化的对话/计划正文替换为中性取消记录，
保留工作区文件和不可变规则快照，并立即释放工作区；若 SQLite WAL 暂被外部读取者占用，任务仍先
进入已取消终态，物理清理标记留待后续启动重试。Provider 凭证必须是 12–16,383 UTF-8 字节的单行值；
若它与这条恢复协议不可避免的字段或字面量冲突，会在配置/Manager 边界直接拒绝，而不是留下无法停止的任务。

切换任务或创建目标时，未发送的任务内容、权限和思考强度会按“直接任务 / 每个项目 /
每个历史任务”分别保留在当前页面内存中；成功提交只清除对应草稿，失败会保留。草稿不会写入
浏览器存储、URL 或后端，刷新或关闭页面即清空。提交尚未返回时仍可切换任务，晚到的成功会加入
侧栏但不会抢回当前页面，晚到失败也不会污染当前任务；同一草稿在请求完成前不能重复提交。

详细事件、差异和证据仍只订阅当前任务。侧栏对未选中的推进态任务做单飞、页面可见时的轻量刷新，
等待用户决策的任务自动降频，全部终态后停止；回到页面时会立即同步一次，因此后台任务完成后无需
切回或刷新整页即可看到新状态。

## 一次任务如何进行

| 阶段 | TraceForge 会做什么 | 你可以做什么 |
| --- | --- | --- |
| 直接答复 | 问候、能力询问、一般解释和明确的只读分析会自然回答，不伪造实施计划或完成证据 | 继续追问，或明确提出要实施的结果 |
| 澄清 | 已确认需要实施，但关键选择仍影响行为、范围或验收时，最多进行两轮确认，每轮 1–3 个问题 | 选择推荐选项或输入自定义答案 |
| 计划 | 生成设计、步骤、影响文件、验收条件和风险，并物化为完整 Markdown | 范围明确的低风险任务自动继续；复杂、高影响或已澄清任务会在实施前请你确认 |
| 执行 | 在工作区内读取、搜索、修改文件并运行明确命令 | 查看实时活动、差异、检查；随时停止 |
| 复核 | 独立只读 Verifier 根据当前轮需求、历史上下文、最终差异和新鲜检查给出结论 | 查看发现；不通过时观察有上限的修复轮次 |
| 证据 | 在最终答复下显示紧凑完成摘要，并汇总最终差异、检查、Verdict、沙箱、事件账本和回滚状态 | 按需打开或下载证据包（Proof Pack） |

一轮结束后，底部输入框可以在同一个任务里继续提要求。新的轮次复用工作区，并把前面轮次的
请求、结果摘要和证据带入上下文；每一轮都可以独立选择动作权限和思考强度，计划确认由任务复杂度自动决定。每条最终答复
下方会列出该轮经 TraceForge 原生编辑工具实际改动的文件，避免把前后多轮的变更混在一起。

如果任务已经回滚，继续输入会创建一个带父任务关联的新任务，而不是复用旧快照。旧任务继续作为
审计历史保留；新任务以当前磁盘内容为权威，建立全新的首次修改快照。因此即使用户在第一次回滚后
手工修改文件，再对续跑任务回滚，也只会恢复到这次手工修改后的版本。一个回滚任务只创建一个线性
续跑任务；相同请求的网络重试返回同一个任务，不同请求会明确冲突，页面也可从旧记录直接打开续跑任务。

任务导航标题稳定取自首次请求的第一句，并按 Unicode 字符安全截断；后续计划或追问不会反复改名。完整原始请求
始终保留在用户消息中，侧栏和任务标题的悬停提示也可查看全文。

### 固定演示与真实模式

`traceforge demo` 是确定性导览：任务文本只读，只接受预置案例，也不能把案例重定向到其他目录。
它不会再把“你好”等任意输入解释为租户缓存任务。要处理真实需求，请直接运行 `traceforge`。

### 自动 Agent、权限与思考强度

这些维度彼此独立，不能互相替代：

- **自动 Agent**：问候或只读问题会直接答复；真正需要执行时先形成结构化计划。单文件、少步骤且只含
  常规本地检查的低风险任务自动继续；多文件、范围不明、关键选择经过澄清、敏感领域或非常规命令会
  展示完整 Markdown 计划并等待确认。常规界面不再提供容易混淆的“计划模式”开关。
- **手动审批（逐项确认）**：读取工具不打断；每次原生文件编辑和命令都先询问。批准计划内
  命令仍保留 OS 沙箱，不会因为“手动点过”就扩大技术边界。
- **自动审批（默认、本地规则）**：计划内写入、认可检查和已知只读命令自动执行；范围漂移和
  未知命令询问。它是确定性的本地策略，不冒充第二个模型审查员。
- **完全访问（工作区）**：工作区与 OS 沙箱内的非高危动作免询问；硬拒绝、真实路径和凭证
  隔离仍不可关闭。未知命令只在 OS 沙箱真实生效时自动执行；`policy_only` 主机会降级为人工
  确认。该高权限选择只对当前轮生效，不保存为下一轮默认值。

三种动作权限都与自动计划确认正交：例如“手动审批”仍会在计划通过后逐项确认动作，
“完全访问（工作区）”也不会跳过复杂任务的计划确认。这里的“完全访问”刻意限定为工作区内，
不等同于 Codex 的宿主机 `danger-full-access`。

澄清、计划审批和动作审批都带有后端生成的请求 ID。页面提交后会立即锁定该张卡片；后端先把
规范化答复写入 SQLite 决策收件箱，再返回 `202 Accepted`。同一请求与同一内容可安全重试，旧请求
或同一 ID 下的不同答复会被拒绝，不能误命中下一轮问题。计划和澄清的“已接收”与状态切换在事务中
消费，并在重启后与发起该问题或计划的原始工具调用精确配对；澄清问题 ID 必须唯一，每个问题也
只能提交一个选项或一条自定义答复。已批准动作会把审批消费、执行态和 `tool.started` 边界一起提交，
再调用外部工具；拒绝动作的确定性工具结果也与审批消费一起提交，崩溃后不会被泛化成“未知失败”。

思考强度是另一项正交维度：它只决定本轮每次模型请求使用的协议档位，不改变计划是否暂停、动作
是否获批、OS 沙箱或 Verifier 权限。所选值会冻结在当前轮，并一致用于 Planner、Builder 和
Verifier；下一轮可以重新选择。官方 OpenAI Chat Completions 路由会发送精确
`reasoning_effort`，官方 DeepSeek 路由会同时按要求设置 thinking 开关；“关闭”会明确禁用
DeepSeek thinking，“模型默认”则完全省略强度字段。TraceForge 不会在服务拒绝后偷偷降档或
删字段重试。

后端 `model.requested` 事件仍把安全的调用元数据写入不可变账本，但默认“工作记录”不再逐条铺开
这些传输细节；右侧时间线只显示通用的“模型调用已记录”，Proof Pack 只记录每轮所选档位。它们都
不显示模型的隐藏推理。DeepSeek 为继续工具调用而要求回传的 `reasoning_content` 只
作为活动轮次的 provider 私有协议状态，不会进入公共 API、事件或证据包。若它包含凭证形态文本，
TraceForge 会安全停止而不是改写后错误回放；正常终态会先清除并确认 SQLite WAL 已截断，再写入
终态。若外部 SQLite 读取者暂时阻止截断，任务会进入可恢复的“已中断”并阻止继续调用模型，
而不会留下 worker 已结束、状态却仍显示运行的幽灵任务；关闭读取者后可停止或继续。启动时也会
清理旧版本遗留在终态记录中的该字段。

“完成后复核”也不是权限模式。它是实现和验收命令结束后的独立只读审查，不能写文件或执行
命令；如果证据不足，会触发有上限的修复轮次。

Builder 的“完成”也是严格协议，不是模型写一句完成即可：`finish` 必须单独调用，摘要必须
非空，所有命令型验收项必须有最新通过记录。默认 30 步限制只计算非终态工具动作；恰好用完动作
预算后仍可提交 `finish`，但不能再偷跑额外动作。混合、畸形或超预算批次会整批拒绝，不会产生
部分副作用，并作为“请求/未执行结果”进入事件账本；连续三次违反协议会明确失败，避免无限重试
或假完成。Builder 完成工作后若误用普通文本代替工具调用，TraceForge 会先给常规纠正，再给一次
明确的原生工具协议纠正，第三次仍无工具才失败；不会把文本伪装成 `finish`。若独立复核在总动作
预算已耗尽后驳回，任务会诚实失败，不会在无法修复时重复提交原结果。

“已答复”和“已证实”是两个不同终态。前者明确表示本轮没有修改文件、运行命令或完成验证，
不会提供当前轮 Proof Pack；后者才表示实施和证据闭环完成。同一个任务可以从任一终态继续
下一轮，而更早成功轮已经冻结的证据不会被后续答复、失败、停止或回滚抹掉。页面会把“本轮完成”
和“历史第 N 轮已证实”分开显示；旧数据若确实没有冻结包，会明确显示“暂无冻结证据”，不会根据
turn outcome 猜一个入口。面向用户的最终答复会先在同一个气泡中流式增长，但此时明确标为临时输出：
后端只解析 `respond_to_user.content` 或 `finish.summary`，每段先跨分片脱敏再写入事件账本。模型响应结束
也只表示“等待提交”；只有 `turn.completed.final_stream_id` 才把该气泡原位提交为正式结果。重试使用新的
流 ID，停止、截断、协议错误、缺少检查或复核驳回都会保留并标记未采纳的临时输出，绝不会与下一次
生成拼接，也不会退回到一次不透明的非流式请求。`started` 事件同时是可恢复的持久化流所有权：进程
重启会在把任务标成中断的同一事务中废弃所有未提交流，继续、停止、失败和回滚也会幂等收口它们。
“已答复”“失败”“已停止”会把 run 状态、关闭后的 turn 和三个终态事件一次提交；数据库故障不会留下
run 已终止但 turn 仍在进行的幽灵状态。模型未遵守工具契约时产生的其他草稿不会提前进入对话，旧版本
已持久化的同轮精确副本也会在显示层安全折叠。如果更早的轮次已有文件快照，
最新问答轮结束后仍可回滚整个任务的已记录改动。

安全中断不再把用户锁进同一次失败重试：页面会同时提供“停止本轮”和“再次继续”，两个请求共享单飞
门禁；停止后可立即在同一任务中输入后续要求。`response_limit` 与流式总大小超限使用明确中文分类，不再
统一暗示连接故障。中断、失败或停止的当前轮若已有真实 `changed_files`，页面会把它们标为“已保留但未
验证”的部分成果并提供打开目录入口；失败命令的错误摘要和已捕获 stdout/stderr 也会同时展示。

模型响应现在先经过统一的 provider ingress，再能进入历史、事件、审批或工具执行：最终答复等纯展示
字段会递归脱敏；工具名、调用 ID、非终态工具参数、完成计划和复核结论等会改变行为的字段只要含有当前
凭证或 `sk-...` 形态就整次拒绝，绝不“脱敏后照样执行”。用户任务、追问、澄清答案和计划反馈同样在
写入前拒绝凭证文本；工具结果的正文、错误和嵌套 metadata 会一起脱敏。所有 SQLite JSON、REST JSON
和 WebSocket 事件使用同一个带语义换行的序列化边界，并在最终字节上复查，因此字段值本身都安全时，
也不能靠引号、逗号或相邻字段拼出凭证。原生文件修改若发现待快照的旧文件或新内容含当前凭证，会在
产生快照或写盘前停止。升级到该边界时，旧版本留下的未消费动作审批会被作废并清除私有协议历史，
不会在恢复后执行。

OpenAI-compatible 流式工具参数同时兼容标准增量片段和单调累计快照：完全重复或带完整前缀的重传只
合并一次，两个不同 JSON 对象仍严格拒绝。非法工具参数、模型输出超限或长度截断不会立即杀死任务；
TraceForge 会在不持久化原始坏参数的前提下，最多按已有模型重试预算加入一次安全纠正上下文并要求
重新生成原生工具调用。成功 SSE 的原始传输总量、事件或帧数越界也走同一有界精简恢复，但每次尝试
仍保留独立硬上限；超大错误响应和畸形协议不会被放宽。`model.retry` 只记录错误类别与恢复策略，不
记录坏响应或隐藏推理；连续失败仍会明确停止，避免宽松提取第一个 JSON 对象造成错误执行。

运行中，对话主线只显示最后一条实时进度；轮次结束后只保留规范最终答复。规划、执行、复核阶段的不同进度不会
丢失，而是完整收进默认折叠的工作记录和事件时间线。旧记录或中断轮次缺少 `turn.completed` 时，仍保留最后一条
可见进度，避免页面无故变空。

### 命令隔离

- macOS 使用 Seatbelt；Linux 在可用时使用非 setuid Bubblewrap。
- 页面和证据包明确区分“系统沙箱已强制”“用户批准的单次绕过”和“仅策略限制”。
- 未知命令在默认模式下需要明确审批；完全访问仅在强制沙箱中免询问；高危系统操作始终拒绝。
- 模型启动的子进程会清理名称中包含 key、password、secret、token、credential 的环境变量。
- 真实项目命令会移除 `PYTHONPATH`、`PYTHONHOME`、虚拟环境标记和 TraceForge 自身虚拟环境的
  `PATH` 入口；只优先使用工作区 `.venv` / `venv`，避免项目测试或安装扫描、修改 Agent 私有
  运行时。即使在 TraceForge 自身目录执行 `uv run`，也会强制切换到不同的工作区本地环境；`pip`
  不能污染安全 base interpreter。只读任务固定、目录一次性的零凭证 Demo 会显式复用随包测试
  运行时，不影响真实任务。
- 强制 OS 沙箱中的命令没有外网。Builder 会优先利用项目既有环境、缓存或零依赖实现；安装因网络
  失败时不会原样反复等待，而会在不偏离计划时切换可用方案，否则明确报告真实阻塞原因。

## 证据与检查怎么读

主区域优先呈现用户请求与 Agent 回复；计划、工具、检查和复核记录收进默认折叠的“工作记录”，
需要时再展开。成功后只在最终回复下保留“本轮已完成、通过几项检查、查看证据”的紧凑摘要，
不再用大型证据板压过交付结果。右侧“任务详情”默认收起，按需打开后提供四个视图：

- **时间线**：按序号查看不可变事件账本。
- **差异**：查看整个任务从首次修改前到现在的累计净 Diff。
- **计划**：阅读或下载完整 Markdown 计划，并查看实时验收证据。
- **验证**：查看完成后独立只读复核的 verdict、证据与修复建议。

每个成功轮次都会生成独立、只写一次的 `traceforge.proof-pack.v2`：成功 Run 状态、已关闭 turn、
三个终态事件和 Proof 行在同一 SQLite 事务中提交，任一环节失败都会整体回滚，因此 API 不会先
暴露一个还没有证据的半成品 `succeeded`。证据范围明确为“累计截至第 N 轮”，包含该轮完成时的
持久差异和事件边界；后续用户手改或新轮次不会重写它。JSON 和 Markdown 都可按成功轮次精确读取，
默认返回最近一次成功证据，并使用 `no-store` 防止浏览器缓存混淆历史。

完整公共 JSON 使用 Artifact SHA-256，语义证据、事件链和 Diff 另有分层摘要，可用于说明“完成
结论依据的是哪一份证据”。这些摘要用于完整性比较，不是数字签名。旧库升级时只回填仍处于当前
成功态、因而还能忠实冻结的记录；已被后续轮覆盖的历史缺口不会拿当前工作区伪造。冻结证据的读取
不依赖当前模型凭证。页面只根据后端返回的真实冻结轮次开放入口，切轮会同时绑定请求、响应和下载
URL；读取失败在弹窗内显示原因并可重试，不会无限转圈。
页面中的事件条数覆盖整个任务；工具动作数和修复轮数明确标为“当前轮”，多轮继续时不会把
逐轮预算计数误写成全任务总量。
切换任务时，页面会立即清空上一任务的事件、Diff 和证据包；后台迟到的 HTTP 或 WebSocket
结果只有在任务 ID 与当前选择及请求代次都一致时才可展示，证据包响应还会校验自身 `run_id`。
主区域运行数据直接从当前选择派生，动作在发出前会再次校验任务所有权；每个任务的表单与加载
状态按 `run_id` 重建，旧任务的失败也不会在新任务页面冒泡。

左侧任务栏和右侧任务详情都可折叠、拖拽调宽，并用版本化本地偏好保存桌面布局；分隔条同时
支持方向键、Home、End 和双击复位。右栏首次使用默认收起，让对话成为主工作区。窗口变窄时，
两侧栏自动切换为独立抽屉，不会覆盖桌面宽度偏好。

界面正文、任务标题和操作入口按照 AIME 的实际字号层级校准，关键文字不低于 10px。窄屏下，
左侧“任务与项目”和右侧“任务详情”会变为键盘可达的抽屉；支持焦点循环和 Escape 返回，
核心功能不会因屏幕宽度而消失。

“本轮更改”只对 `create_file`、`apply_patch` 做落盘前后指纹比较，包含多文件编辑中途失败后
已经真实写入的文件，也会跨失败和重启持久化。任意 `run_command` 仍可能在工作区产生副作用；
在没有全工作区审计前，界面不会把原生编辑工具列表夸大成所有命令副作用的完整清单。

## 停止、断线、恢复与回滚

### 停止

“停止”只阻止后续动作，已经写入的文件修改会留在工作区。你可以先检查差异，再决定是否
回滚；TraceForge 不会把“停止”和“撤销文件”混为一谈。

若暂停任务的旧上下文恰好与后来轮换的 provider 凭证相同，普通重存会被凭证守卫拒绝。此时
“停止”使用上述专用恢复路径，不调用模型或工具，不让任务永久占用工作区；历史不可变事件不会
被静默改写，公开读取仍经过当前凭证脱敏。

### 模型连接中断

连接、超时、限流或临时服务错误会进行有界、可见的重试。连续失败后，任务进入“安全暂停”：

1. 工作区、计划、消息和事件全部保留。
2. 暂停期间可以修改模型设置。
3. 点击“继续”后从持久化状态恢复；未完成的命令不会被猜测或自动重放。

恢复仍沿用中断轮次冻结的思考强度。如果新模型路由不支持该档位，TraceForge 会保持“已中断”并
明确要求改回兼容路由，而不是静默降级后继续。

### 进程重启

程序意外退出后，未完成任务会标为“已中断”。待答复或已接收的澄清、计划审批和动作审批会保留
精确请求 ID；用户明确点击“继续”后，待答复卡片重新出现，已接收答复则从持久收据继续消费。
已批准但尚未进入执行事务的动作只执行一次；一旦 `tool.started` 与审批消费已原子提交，重启后
若缺少完成事件就标记为 `uncertain`，绝不自动重放，恢复模型只能先检查当前工作区。用户主动停止
仍会把未消费决策记为 `abandoned`，旧 ID 不能授权后来动作。
同一应用数据目录由 owner-only 单实例锁保护；重复启动会复用健康的同版本实例，而不是再次构造
应用并误改活跃任务。复用还必须匹配随机实例身份与启动配置；第二个进程会有界等待首个进程
发布身份和健康状态，旧版无锁实例则会被明确拒绝，不能换端口绕过。端口若被其他服务占用，会在
打开数据库和恢复运行前失败，并给出可复制的备用端口命令。IPv4 与 IPv6 回环地址均使用同一套
本地来源和实例身份校验。

### 回滚

回滚需要二次确认。TraceForge 保存每个首次修改文件的原始版本和哈希；如果用户之后又改过
某个文件，该文件会作为冲突保留，其他未冲突文件仍可安全恢复。
弹窗在请求完成前保持打开并锁定，失败会就地显示且可重试；成功后页面列出已恢复、已移除和保留
冲突的文件。回滚与继续操作共用任务生命周期锁，并在触碰文件前永久作废未消费的人工作答；每个
文件先识别“已经恢复/已经移除”的目标状态，因此中途失败可安全重试，同时仍拒绝覆盖用户后改内容。
文件处理成功后，`rolled_back` 状态与完整结果事件在同一 SQLite 事务中提交；服务端已提交但响应
丢失时，相同重试直接返回第一次持久化结果，不会再次触碰文件。回滚后的继续工作使用上文所述的
新任务和新快照边界。

## 启动前自检

```bash
# 普通检查：缺少模型配置只给警告，因为仍可进页面设置
uv run traceforge doctor --port 8765

# 严格检查：要求系统沙箱，并真实探测一次模型原生工具调用
uv run traceforge doctor \
  --port 8765 \
  --require-os-sandbox \
  --probe-model
```

自检覆盖直接任务根目录与状态目录写入、SQLite 初始化/迁移、前端资源、端口、系统沙箱和凭证
来源，不会打印密钥值。高级用户仍可传 `--workspace /绝对路径` 专项检查或覆盖直接任务根目录；
也可以通过 `TRACEFORGE_WORKSPACE_ROOT` 设置持久的非默认位置。

## 当前功能状态

| 能力 | 状态 | 最近的可核验证据 |
| --- | --- | --- |
| 指定 DeepSeek 模型真实任务闭环 | 已完成 | 单文件修复、多文件修复和空目录 greenfield 项目均为 verifier pass、Proof Pack proven；最新真实 UI 任务生成 4 个项目文件和 11 个测试，harness 另以四个 CLI 子命令黑盒复查持久化与 README |
| 主流 Coding Agent 对齐后的对话工作台 | 已完成 | 对话主线、折叠 Trace、完整 Markdown 计划、窄屏抽屉、Chrome 与 WCAG 基线通过 |
| 直接任务与项目任务 | 已完成 | 直接任务隔离目录、macOS 原生选项目、折叠项目层级和历史关联均有回归 |
| 固定演示边界 | 已完成 | 任意任务与目录重定向均被 API 拒绝；浏览器中预置任务只读 |
| 按复杂度自动确认计划 | 已完成 | 常规界面统一为 Agent；低风险单文件计划自动继续，复杂、高影响或已澄清任务在实施前确认 |
| 手动 / 自动 / 工作区完全访问 | 已完成 | 逐轮冻结、旧库迁移、软/硬权限矩阵、无沙箱降级、过期审批与取消清理均有对抗回归 |
| 同任务多轮对话 | 已完成 | 同一 run/workspace 连续两轮、上下文继承、turn 事件和 API 回归 |
| 计划外动作与命令权限 | 已完成 | 计划外写入、未知命令、高危拒绝及单次绕过测试 |
| 完整 Markdown 计划 | 已完成 | 结构化契约生成设计/步骤/文件/验收/风险文档，页面可读且可下载 |
| 系统命令沙箱与可审计降级 | 已完成 | Seatbelt 对抗测试；Linux Bubblewrap 实现与探测 |
| Proof Pack 与冲突感知回滚 | 已完成 | 逐成功轮不可变 v2、原子成功事务、历史选择、分层摘要、旧库诚实回填及部分冲突回滚测试 |
| 进程/模型故障恢复 | 已完成 | 可见重试、坏工具参数/输出超限有界重生成、修改配置后恢复、未完成工具不重放、无幽灵运行 |
| `traceforge doctor` 启动预检 | 已完成 | READY/NOT READY 故障注入与 CLI 测试 |
| 无参数启动与应用内选择项目 | 已完成 | `traceforge` 直接启动、隔离临时目录、原生项目选择与 API 对抗测试 |
| 前端与 Agent 默认中文 | 已完成 | 三个模型角色均有简体中文契约；静态界面、状态、提示和无障碍名称均为中文 |
| 按模型动态解析上下文 | 已完成 | 精确 endpoint/model 目录、手动覆盖、保守回退、逐运行快照和压缩边界回归 |
| 按模型逐轮选择思考强度 | 已完成 | 精确 route/model 能力目录、OpenAI/DeepSeek 协议映射、逐轮冻结、恢复阻断与隐藏推理防泄漏回归 |
| 自然答复与执行意图区分 | 已完成 | Agent 支持结构化直接答复；问候不澄清、不计划、不生成虚假证据 |
| 对话优先的完成态与逐轮文件 | 已完成 | 大型证据板已替换为紧凑摘要；两轮文件集合、部分失败持久化和历史 Proof v2 均有回归 |
| 可折叠、可调宽的双侧栏 | 已完成 | 右栏默认收起；鼠标、键盘、刷新持久化、恶意偏好回退和 390–1366 px 断点均通过浏览器验收 |
| 在系统文件管理器中定位任务 | 已完成 | run-scoped API、固定 argv、凭证环境清理、跨站防护、路径替换和 Linux 降级测试 |
| 天枢交互取舍 | 已完成 | 固定源码版本审计；吸收对话/轨迹渐进披露，不复制固定多栏、默认推理或启发式文件清单 |
| 跨任务界面与证据隔离 | 已完成 | 双任务反向响应、慢加载动作归属、迟到错误和已脱离事件流均有确定性回归 |
| 精简模型调用展示 | 已完成 | 调用事件保留在账本，默认 Trace 隐藏传输噪声，时间线使用通用标签 |
| 精确模型离散档位控件 | 已完成 | 固定 Harness 源码对标、稀疏能力原序选择、单档静态态和 390 px 浏览器验收 |
| 单一规范最终答复 | 已完成 | Planner/Builder/Verifier 终局草稿不公开，旧同轮精确副本兼容投影 |
| 单实例安全启动 | 已完成 | 锁持有、实例身份/配置匹配、启动窗口等待、旧版实例保护、IPv6、端口冲突不构造应用及活跃状态不变的对抗回归 |
| 严格完成协议与动作预算 | 已完成 | 畸形/混合/超预算整批拒绝、连续 prose-only 两阶段纠正、精确边界、复核驳回、恢复配对和连续错误熔断回归 |
| 原子连接验证与可信就绪 | 已完成 | 失败草稿零持久化、旧连接保留、同配置失败失效及三类任务入口后端门禁 |
| 简洁任务标题与单一实时进度 | 已完成 | 稳定首句标题、终态单一答复、不同进度移入 Trace、添加项目直达输入页的单测与 Chrome 回归 |
| 耐久化用户决策与回滚续跑 | 已完成 | 请求绑定收件箱、原子动作起点、崩溃恢复、单次提交、回滚结果和新快照 lineage 的故障注入及 Chrome 回归 |
| 可恢复、可审计的流式最终输出 | 已完成 | 跨分片脱敏、持久化 stream generation、重启/重试/取消/截断隔离、原子终态、单气泡提交、滚动保护及窄屏 Chrome 回归 |
| Provider、持久化与公开输出的凭证闭环 | 已完成 | 语义字段拒绝、展示字段脱敏、结构拼接防护、SQLite/API/WS 最终字节检查、旧审批作废、快照阻断和对抗回归 |
| 首次模型接入向导 | 已完成 | 官方/自定义预设与独立草稿、跨语言端点契约、完整模型建议、文件/环境凭证切换、缺凭证门禁及 WCAG Chrome 回归 |
| 多任务切换连续性 | 已完成 | 后台元数据自适应刷新、完整会话草稿隔离、提交所有权、迟到响应与双击防重浏览器回归 |
| 工作区根规则感知 | 已完成 | 逐轮不可变快照、恢复复用、工具前置绑定、轮换凭证安全停止、私有原文边界、Trace/时间线和对抗回归 |
| 空项目执行环境隔离 | 已完成 | 宿主 Python 路径、直接解释器和 `uv run` 均无法借用 Agent 私有环境；base Python 禁止 pip 污染；标准库 unittest 可作为认可检查 |
| 轻量浅色工作台 | 已完成 | 首屏移除常驻诊断噪声，权限改为紧凑分段控件；桌面/移动 Chrome 与 WCAG A/AA 回归通过 |
| 工作台关键交互细节 | 已完成 | 品牌固定左侧、思考选择保持浅色 hover、侧栏折叠不再压缩主区，计划确认改为复杂度自动触发 |
| 中断后的可控恢复与部分成果 | 已完成 | 停止/再次继续单飞、输出超限中文分类、失败命令双层诊断、三类非成功终态的保留文件与目录入口均有 Chrome 回归 |
| 凭证化真实模型验收隔离 | 已完成 | 模型改写后的测试与隐藏 oracle 强制 OS 沙箱、凭证路径遮蔽、签名代理清理、1 MiB 输出上限、超时进程组回收与恶意 DB 取密回归 |

当前质量基线：519 个后端测试通过，覆盖率 87.52%（硬门槛 85%）；35 个前端单测、38 个串行 Chrome 场景、WCAG A/AA
自动扫描、390–1366 px 及展开详情后的窄主区响应式检查和 Seatbelt 严格质量语料全部通过。

## 当前边界

TraceForge v0.1 不做 Windows、多用户、云执行、浏览器自动化、宠物、插件市场、IDE 克隆和
并行多 Agent 写入。当前不支持二进制文件编辑。Linux 的系统隔离依赖主机安装并允许使用
Bubblewrap；不可用时页面会诚实显示“仅策略限制”。

这些边界是当前产品取舍，不是隐藏限制。只有真实使用证明某项边界阻碍核心工作流时，才进入
需求评估。

## 需求追踪

需求的权威入口是项目协作 Wiki；本文档同步已经落地的用户能力和当前阶段，便于在仓库中
随时查阅。

| ID | 需求 | 状态 | 落地证据 |
| --- | --- | --- | --- |
| TF-001 | 使用指定真实模型，做成可实际使用的产品 | 已完成 | 真实模型修复与空目录项目三场景、配置入口、连接探针与恢复闭环 |
| TF-002 | 参考 AIME 改善前端，但不照抄通用 Agent 平台 | 已完成 | 证据工作台、阶段折叠、响应式与可访问性 |
| TF-003 | 支持直接任务，以及打开/创建项目后的项目任务 | 已完成 | 工作区路由、项目持久化和完整浏览器验收 |
| TF-004 | 参考 DeepSeek Harness、Codex 等 SOTA 工程实现 | 已完成 | 固定源码版本对标及核心可靠性取舍落地 |
| TF-005 | 在项目中维护用户友好的功能与用法文档 | 已完成 | 本文档及仓库维护约定 |
| TF-006 | 前端默认语言为中文 | 已完成 | 中文静态界面与自动化浏览器验收 |
| TF-007 | 参考 Codex 简化直接任务与项目创建入口 | 已完成 | 自动隔离目录、原生选项目、折叠层级及后端/浏览器回归 |
| TF-008 | 普通输入不能误入固定租户缓存演示 | 已完成 | 演示模式显式化、任务与目录 API 对抗测试、只读预置任务 |
| TF-009 | 创建页中点击历史任务必须直接返回运行详情 | 已完成 | 选择任务同步退出创建态，Chrome 回归覆盖 |
| TF-010 | 对齐 AIME 可读性并保留窄屏功能入口 | 已完成 | 实测字体层级、零关键 6–9px 文本、双抽屉键盘与 WCAG 回归 |
| TF-011 | 计划应是完整 Markdown 文件，而不是简单步骤列表 | 已完成 | 规范化 Markdown 契约、下载 API、自动计划确认和 Chrome 验收 |
| TF-012 | Trace 可见但不被流程限制，并支持同任务多轮 | 已完成 | 对话主线、折叠 Trace、持久化 turn、同 run follow-up API 与回归 |
| TF-013 | 计划确认应由任务复杂度自动触发，不保留冗余开关 | 已完成 | 常规入口统一为 Agent；确定性复杂度门决定自动继续或实施前确认 |
| TF-014 | 区分计划确认、动作权限与完成后独立复核 | 已完成 | 三套语义和 UI 文案分离；复杂计划、范围漂移、命令审批、只读复核各自测试 |
| TF-015 | 启动不再要求 workspace 参数，工作目录在应用内确定 | 已完成 | 无参数启动、可见默认根、项目选择和无目标 API 隔离回归 |
| TF-016 | 上下文容量应随模型动态适配，不能盲目固定为 64k 或 1M | 已完成 | 容量来源可见、官方端点精确匹配、自定义覆盖、运行快照与上下文压缩测试 |
| TF-017 | “你好”等普通输入不应误入澄清或伪造编码流程 | 已完成 | `answered` 独立终态、结构化直接答复、混合终局调用拒绝和 API/UI 回归 |
| TF-018 | Agent 面向用户的回答默认使用中文 | 已完成 | Planner、Builder、Verifier 统一简体中文输出契约，并保留代码、命令和路径原文 |
| TF-019 | 参考天枢中值得借鉴的交互方式 | 已完成 | 固定提交源码与实机审计；落实对话优先、折叠轨迹和按需详情，并记录拒绝照搬项 |
| TF-020 | 弱化大型证据板，完成后显示本轮改动文件 | 已完成 | 紧凑完成摘要、逐轮真实编辑文件、累计 Diff 语义和异常持久化测试 |
| TF-021 | 左右侧栏可折叠、拖拽调宽，右栏默认收起 | 已完成 | 版本化布局偏好、无障碍分隔条、联合宽度约束和移动抽屉回归 |
| TF-022 | 可从任务中打开本地工作目录 | 已完成 | macOS Finder、Linux 文件管理器能力降级、run-scoped 防替换 API 与跨站保护 |
| TF-023 | 支持手动审批、自动审批和完全访问三种动作权限 | 已完成 | 与自动计划确认正交的逐轮选择器、SQLite/API 快照、权限证据徽标、沙箱降级和硬拒绝对抗测试 |
| TF-024 | 支持思考强度选择并与主流产品协议对齐 | 已完成 | 精确模型能力选项、逐轮一致传输、默认省略、DeepSeek 私有回放、恢复兼容检查和 UI/安全回归 |
| TF-025 | 快速切换任务时不能操作旧任务或混淆界面、错误与证据 | 已完成 | 派生当前运行、动作所有权、请求代次和响应 `run_id` 校验；五项确定性 Chrome 竞态回归 |
| TF-026 | 默认 Trace 不应暴露大量模型请求与协议字段细节 | 已完成 | 原始事件继续审计；默认工作记录移除调用噪声，时间线使用通用中文投影 |
| TF-027 | 思考强度控件应像主流产品一样精致且不误导 | 已完成 | 对标 Harness 固定提交；原生离散胶囊选择器严格使用精确模型稀疏能力，单一能力显示静态态 |
| TF-028 | 直接答复不能把同一答案显示两次 | 已完成 | 三阶段只发布已接受的非终态进度；最终答复单一规范事件，旧数据同轮精确去重 |
| TF-029 | 重复启动或端口冲突不能误中断正在运行的任务 | 已完成 | owner-only 单实例锁、构造应用前预绑定、实例身份/配置精确复用、启动窗口接管、旧版实例保护、IPv6 及活跃状态不变回归 |
| TF-030 | Builder 不能用畸形或混合 `finish` 制造假完成，步数边界也不能误判 | 已完成 | 严格终止 schema、终态独占批次、非终态动作预算、精确边界与熔断测试 |
| TF-031 | 连接失败不能保存坏草稿或把“有凭证”显示成“可运行” | 已完成 | 内存探测后原子保存、失败 Key 零落盘、验证时间戳、前后端入口门禁与浏览器回归 |
| TF-032 | 高频任务流应易识别、少日志气泡，添加项目后不应停在原页面 | 已完成 | 稳定标题、实时进度收束、终态单一答复、项目输入归属和真实浏览器回归 |
| TF-033 | 同任务后续轮不能抹掉或误标早先成功轮的完成证据 | 已完成 | 逐成功轮原子冻结、精确历史 API/下载、真实可用性索引、旧库保守回填、反向响应与窄屏错误恢复回归 |
| TF-034 | 人工决策不能因重试或重启串到下一请求，回滚后续跑也不能复用旧快照 | 已完成 | 请求 ID + SQLite 决策收件箱、审批/动作起点原子事务、uncertain 不重放、线性 successor lineage 及浏览器并发回归 |
| TF-035 | 面向用户的最终答复应流式输出，同时保持证据、脱敏与恢复语义可信 | 已完成 | 逐段持久化、全边界及相邻凭证脱敏、重启/尝试隔离、原子规范提交、断线重放、滚动控制和端到端回归 |
| TF-036 | Provider 回显或 JSON 结构拼接不能把凭证写入历史、事件、审批、快照或公开响应，更不能带凭证执行工具 | 已完成 | 单一 ingress、语义 fail-closed、最终序列化复查、存储守卫、REST/WS 投影、旧审批迁移及 PENDING/ACCEPTED 不执行回归 |
| TF-037 | 首次模型连接不应要求用户自行理解模型与接口配对，也不应通过一次必败请求才发现缺凭证 | 已完成 | 官方/自定义预设与草稿恢复、完整已适配模型建议、前后端一致的原始端点边界、复合凭证来源及缺凭证门禁 |
| TF-038 | 多任务切换不能让后台状态陈旧、丢失未发送草稿，或让晚到请求抢占当前页面 | 已完成 | 选中任务专属事件流、后台元数据自适应刷新、内存草稿注册表、提交所有权与迟到成功/失败/双击回归 |
| TF-039 | 项目应感知工作区规则，但规则不能静默扩权、跨恢复漂移或泄漏原文 | 已完成 | 仅根 `AGENTS.md`、逐轮原子私有快照、低优先级三角色注入、变更前绑定、轮换凭证安全停止、公开安全清单及窄屏/WCAG 回归 |
| TF-040 | 简单项目不能因宿主环境污染、无效依赖探测或非法工具历史而失败 | 已完成 | 并行工具结果原子相邻、批后恢复提示、命令临时目录规范化、Python/`uv run` 私有环境隔离、离线安装换路及失败链路回归 |
| TF-041 | 界面应减少说明和常驻诊断信息，并达到主流 Coding Agent 的轻量交互水准 | 已完成 | 浅色 Cloud IDE 视觉、紧凑首屏与模式控件、按需证据层级、桌面/移动/WCAG 浏览器验收 |
| TF-042 | 修复思考控件、品牌位置、侧栏折叠，并让计划确认按复杂度触发 | 已完成 | 浅色 hover、显式 Grid 布局、统一 Agent 入口、低风险/复杂计划状态机与 Chrome 回归 |
| TF-043 | DeepSeek 不能因累计流片段、坏工具 JSON、超限响应或完成时未调用 `finish` 直接把任务跑死 | 已完成 | 累计/增量流合并、差异对象拒绝、有界模型纠正、两阶段完成协议恢复、真实 greenfield 项目与隐藏检查通过 |
| TF-044 | Greenfield 真实模型验收不能用内部存储 API 或模型自测代替可运行的 CLI 与用户文档 | 已完成 | harness 以四个独立 `main.py --data` 子进程串联 add/list/toggle/delete，跨进程复查 JSON 状态，并要求 README 为每个子命令提供可执行示例 |
| TF-045 | 中断或失败后必须能停止重试、继续对话并找到已经落盘的部分成果 | 已完成 | 中断双操作与单飞、超限分类、失败命令 error/output 并列、当前轮 changed_files 和目录入口的浏览器回归 |
| TF-046 | 真实模型验收不能让模型生成的代码借后置测试读取凭证、使用 Agent 私有运行时或耗尽宿主资源 | 已完成 | 后置测试 fail-closed OS 沙箱、生产环境清理、symlink/shebang 拦截、输出/超时进程组上限及凭证 DB 对抗回归 |

### 提新需求时建议写清楚

```text
【新增 / 优化 / 问题】一句话标题
使用场景：什么时候会遇到？
期望结果：完成后应该是什么体验？
验收方式：你准备如何确认它真的可用？
不需要做：明确不想扩大的范围（可选）
```

## 维护约定

- 用户可见功能、状态语义、操作步骤或产品边界改变时，同一提交必须更新本文档。
- 只有实现、针对性测试和实际操作证据都成立，功能状态才改为“已完成”。
- 新需求先进入 Wiki；开始 phase 前完整复读全部待办，再同步到本文档的需求追踪表。
- 不在本文档记录凭证值、私人路径或临时调试数据。
- 打包和录制不是当前阶段目标，除非协作 Wiki 明确新增对应待办。
