# Fault-injection evidence

TraceForge treats recovery as a product behavior, not a promise made by the model. The deterministic
tests below inject failures at the boundaries most likely to matter during a live demonstration.

| Injected failure | Required invariant | Persisted evidence | Automated coverage |
| --- | --- | --- | --- |
| Process disappears during an unfinished run, including after a stream starts or provider-completes | Startup changes the run to `interrupted` without guessing that it completed and atomically closes every uncommitted stream generation | `assistant.output.aborted` precedes `state.changed`, which records the previous state and `cause=process_restart` | `test_mark_active_runs_interrupted`, `test_mark_all_active_runs_interrupted`, and `test_process_restart_atomically_aborts_uncommitted_stream_generation` |
| A second launch overlaps the owner's pre-publish/pre-health window, the owner exits, an older no-lock instance is live, or an unrelated service owns the port | Reuse only an exact healthy instance, take over only after lock release, and otherwise fail before constructing FastAPI or rewriting unfinished runs | Owner-only lock metadata binds a random identity and launch fingerprint to the live URL; existing run rows and events remain unchanged | `test_busy_lock_waits_for_owner_to_publish_before_reusing`, `test_busy_lock_retries_health_until_published_owner_is_ready`, `test_busy_lock_takes_over_when_starting_owner_exits`, `test_custom_port_startup_refuses_a_legacy_default_instance`, `test_bind_race_with_a_legacy_instance_never_suggests_a_parallel_port`, and `test_occupied_port_fails_before_serve_or_demo_constructs_an_app` |
| A provider returns malformed `finish`, mixes it with actions, or requests more actions than remain | Reject the whole batch, pair every call with an error, preserve public-progress integrity, and still allow a legal `finish` at the exact action limit | Each rejected call gets `tool.requested` plus `tool.completed` with `protocol_rejected/not_started`; no `tool.started`, side effect, or public progress is emitted | `test_builder_rejects_malformed_finish_before_verification`, `test_builder_rejects_entire_mixed_finish_batch_and_preserves_public_progress`, and `test_builder_rejects_over_budget_batch_before_executing_any_call` |
| A multi-tool batch is interrupted after only its first result, or verification rejects at the exact action limit | On resume, synthesize results only for missing call IDs; never replay either action. If no repair action remains, fail instead of resubmitting an unchanged result | `run.resumed` counts only newly repaired protocol gaps; exact-limit rejection has no false `repair.started` event | `test_resume_repairs_only_the_missing_result_in_a_partial_tool_batch` and `test_exact_action_budget_verifier_failure_stops_without_phantom_repair` |
| A draft provider route or raw key fails its native tool-call probe | Keep the prior verified configuration intact, persist none of the rejected key, and keep a fresh install gated | Provider row, managed-key set, public readiness, and task rows remain unchanged; returned detail is redacted | `test_failed_draft_probe_keeps_verified_config_and_never_writes_new_key`, `test_fresh_failed_provider_probe_does_not_create_config_or_managed_key`, and the failed-draft browser scenarios |
| The managed-credential directory is replaced by a symlink, or a user supplies a similarly named credential in the data root | Refuse symlink write-through and delete only files proven to live in the dedicated managed directory (plus the exact legacy path) | Provider row remains unchanged on rejection; external target and manual credential bytes remain intact | `test_managed_credential_directory_symlink_is_rejected_without_write_through`, `test_replacing_a_manual_root_credential_never_deletes_it`, and `test_replacing_the_exact_legacy_managed_credential_removes_it` |
| Model connection, timeout, rate limit, or temporary server failure persists across bounded retries | Preserve the workspace and history in `interrupted`; allow connection settings to change and resume without starting over; a simultaneous Stop still closes the turn on the first request | `model.retry` records every application-owned retry; recoverable `error`, `state.changed`, and `run.resumed` record the pause and continuation | `test_transient_model_outage_pauses_and_resumes_without_losing_run`, `test_cancel_reloads_run_after_provider_interrupts_during_tool_cleanup`, and the provider-repair API case |
| A streamed response disconnects after visible output, is cancelled, retried, or fails private-state validation after provider completion | Preserve the redacted partial as explicitly non-canonical; give every retry a new identity and close every rejected durable owner | `assistant.output.aborted` records retry/cancel/interruption/failure status; `(stream_id, segment_index)` stays isolated and only `turn.completed.final_stream_id` commits a bubble | `test_retry_uses_distinct_streams_and_commits_only_the_successor`, `test_cancelling_a_live_stream_persists_an_aborted_partial`, `test_post_stream_private_reasoning_rejection_aborts_the_durable_draft`, and the streaming browser replay scenario |
| A provider splits adjacent credentials across chunks, uses an escaped or marker-shaped key, truncates JSON, sends an unsafe terminal shape, continues after completion, sends heartbeats forever, compresses or exceeds a body/field/global bound | Release only prefix-stable redacted text, bound bytes before JSON, close transport on timeout/cancel/boundary failure, and fail closed without a fallback | Public delta/completion events and SQLite contain no configured key, token-shaped secret, escaped key, or leaked prefix; unsafe shapes have no canonical link | every-prefix/marker/escaped redaction cases, truncation and raw-transport boundary tests, wall-clock timeout, real raw cancellation, and unsafe-protocol/global-budget tests |
| An interrupted turn with explicit reasoning effort is pointed at a route that does not support it | Keep the run `interrupted`, preserve the frozen turn setting, and reject resume before any model request instead of silently downgrading | Existing turn and `reasoning_effort` remain unchanged; no false `run.resumed` event or provider call is created | `test_resume_preserves_effort_and_blocks_an_incompatible_new_route` |
| An external SQLite reader prevents private-reasoning WAL truncation, or the interruption state event write fails | Bound cleanup, atomically close stream/error/state, keep the scrubbed row recoverable, and roll back to the prior active state if that transaction fails | `error` and `state.changed` record `cause=provider_reasoning_cleanup_pending`; the pending flag survives until a verified checkpoint; fault injection leaves neither a partial abort nor an eventless interrupted row | cleanup/checkpoint tests plus `test_interruption_commit_fault_rolls_back_stream_state_and_events_together` |
| Provider violates its response contract, an unexpected exception occurs, or terminal persistence fails after constructing an answer | End with one atomic failed/answered/cancelled turn or remain recoverably non-terminal; never expose a terminal run with an in-progress turn | The RunRecord plus `state.changed`, `turn.completed`, and `run.completed` share one guarded transaction | `test_unexpected_provider_exception_never_leaves_a_ghost_run`, `test_atomic_terminal_turn_rolls_back_state_turn_and_events_together`, and `test_answer_commit_fault_cannot_leave_a_terminal_run_with_an_open_turn` |
| Planner emits prose before correcting itself to a terminal tool, or a builder/verifier response carries rejected or terminal prose | Publish only progress backed by accepted non-terminal tools and one canonical terminal result; never show the same answer twice | Invalid/terminal drafts stay in internal protocol history and create no public `message`; `turn.completed` is authoritative | `test_planner_prose_correction_publishes_only_the_canonical_answer`, `test_planner_read_progress_remains_visible_before_the_answer`, `test_terminal_builder_and_verifier_drafts_are_not_published`, and `test_builder_publishes_only_progress_backed_by_accepted_tools` |
| Provider history ends with one or more unmatched calls in its latest tool batch | Resume closes only the missing protocol gaps with synthetic failures and never replays either completed or unknown side effects | `run.resumed` records the prior phase, selected strategy, and newly repaired call count | `test_resume_repairs_only_the_missing_result_in_a_partial_tool_batch` |
| Process stops after a clarification/plan/action response is durably accepted but before the worker consumes it, including when the provider reused a call ID | Preserve the exact request and payload; only explicit resume may consume it, and pair the reply with the latest unanswered source tool call rather than an older matching ID | `decision_requests` remains `accepted`; resume strategy identifies the decision kind; the paired tool result and resolved event retain request/subject identity | `test_accepted_clarification_crash_window_pairs_answer_exactly_once`, `test_restarted_clarification_rounds_ignore_reopen_events_and_reused_call_ids`, `test_accepted_plan_revision_after_restart_pairs_submit_plan_call`, and accepted action/retry tests |
| User cancels while a human decision is pending or accepted | Mark that exact receipt abandoned and never let its ID authorize a later prompt/action | `decision.abandoned` or `approval.resolved` records `outcome=abandoned` and `cause=user_cancelled` | cancellation plus stale-ID decision tests |
| Process stops after an approved action's durable start boundary but before completion | Never replay the action; mark the receipt uncertain and force inspect-first recovery | Approval consumption, `state.changed`, and `tool.started` share one transaction; resume records `uncertain_action_approvals` | `test_approved_action_consumption_and_start_marker_are_atomic` and `test_started_approved_action_is_uncertain_and_never_replayed_after_crash` |
| Process stops while persisting an action rejection | Preserve the exact user-rejected tool result; never substitute a generic protocol-repair error | Decision consumption, deterministic tool result, and `tool.completed` evidence share one transaction | `test_rejected_action_result_and_decision_consumption_are_atomic` and `test_rejected_action_result_survives_crash_after_decision_consumption` |
| Independent verifier rejects a result after a passing check | The repair edit invalidates the old check; `finish` stays blocked until the exact check passes again | `repair.started`, a pending check update, and two distinct command result events | `test_repair_cannot_reuse_a_passing_check_from_before_the_edit` |
| A multi-file native patch writes its first file and the second write raises | Report and persist only the file that actually changed, even when repeated failures terminate the turn | failed `tool.completed` metadata, `diff.updated`, and terminal `turn.completed.changed_files` all retain the partial mutation | `test_mutation_metadata_reports_only_files_that_actually_changed` and `test_partial_edit_files_survive_terminal_builder_failure` |
| User edits one of several agent-touched files before rollback | Preserve the conflicting file while safely restoring every unchanged agent version | `rollback.completed` separates `restored`, `removed`, and `conflicts` | `test_rollback_restores_safe_files_while_preserving_one_conflict` |
| Rollback is requested while a decision or provider-completed stream is open, races Resume, fails mid-file batch, or crashes before terminal DB commit | Abandon old decisions and drafts before files; allow only one lifecycle operation; recognize already-restored paths on retry; close an interrupted turn with the terminal result | Decision/stream aborts precede file work; lifecycle conflicts are explicit; `rolled_back`, optional `turn.completed`, and `rollback.completed` commit in one SQLite transaction after idempotent file convergence | `test_rollback_of_interrupted_stream_aborts_draft_and_closes_active_turn`, `test_rollback_abandons_every_interrupted_human_decision_before_files`, race/retry tests, and storage rollback atomicity tests |
| User rolls back, edits a restored file manually, continues, then rolls back again | Give continuation a new run/snapshot namespace and restore the post-parent-rollback manual version, never the original older baseline | `run_lineage` binds one successor; its `turn.started` identifies the parent and its snapshots contain the new baseline | `test_rollback_successor_uses_fresh_snapshot_boundary_and_lineage` and the linked-successor API/browser scenarios |
| Browser double-submits a decision/follow-up, a rollback fails, or its successful response is lost | Dispatch one request, keep controls locked while pending, preserve local error/results, and make rollback/successor retries idempotent without touching files twice | Request-bound decision receipt, unique parent lineage, and the first persisted rollback result remain authoritative | `durable-decisions.spec.ts`, rollback-successor retry assertions, and `test_api_follow_up_after_rollback_creates_a_linked_successor` |
| Run A's response or event is delayed across a switch to B, or B's Diff remains pending | Render and act only on B; never leak A's error, local loading state, Diff, event, Proof, or destructive control target beneath B's selection | Visible run derives from selection; actions, errors, request generations, inbound event owner, WebSocket Diff epoch, and Proof `run_id` agree before use | Five `artifact-isolation.spec.ts` scenarios wait for response completion plus browser commits and inject a stale socket without timing guesses |

TraceForge disables the OpenAI SDK's hidden retries, applies a bounded per-attempt timeout, and
records its own retry decisions. Exhausting a retryable failure pauses rather than fails the run;
an interrupted run has no live worker, so the user may repair provider settings and then resume.
Resume first validates the active turn's frozen reasoning effort against the newly selected exact
route/model capability. Provider repair may change the endpoint or model, but it cannot silently
change the semantic request made by the interrupted turn.

`run.resumed` is emitted before resumed work continues. Its strategy is application-selected:
clarification and Plan-mode approvals return to their exact human gate, a persisted Agent
auto-continue decision resumes without inventing a click, and accepted decisions are consumed from
their stored payload. An accepted action with no start marker may execute once after explicit
resume. Once the atomic start marker exists, a missing completion is classified as uncertain and
never replayed. `repair.started` records the bounded cycle number and the verifier finding that
caused it. These events enter the same append-only sequence hashed by the Proof Pack.

These tests primarily inspect SQLite state, exact event
payloads, model-visible protocol repair, file contents, and command-event counts, so failures are
reproducible without an API key. The WAL-busy case additionally uses a generous upper bound to
catch accidental reintroduction of SQLite's multi-second lock wait. Event-broker pressure is also adversarially covered: a slow
subscriber may lose bounded in-memory queue entries, but cannot fail execution, and WebSocket
sequence gaps are replayed from the authoritative SQLite ledger.
